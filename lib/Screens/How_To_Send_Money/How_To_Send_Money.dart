// ignore_for_file: file_names, prefer_const_constructors

import 'package:flutter/material.dart';

class HowToSendMoney extends StatelessWidget {
  const HowToSendMoney({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'How to send money',
        ),
      ),
      body: Container(),
    );
  }
}
