// ignore_for_file: file_names, prefer_const_constructors

import 'package:flutter/material.dart';

class ProblemsWithMyBankOrBankAccount extends StatelessWidget {
  const ProblemsWithMyBankOrBankAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Problems with my bank or bank account',
        ),
      ),
      body: Container(),
    );
  }
}
