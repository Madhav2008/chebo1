// ignore_for_file: file_names, prefer_const_constructors

import 'package:flutter/material.dart';

class IssuesWithPayments extends StatelessWidget {
  const IssuesWithPayments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Issues with payments',
        ),
      ),
      body: Container(),
    );
  }
}
