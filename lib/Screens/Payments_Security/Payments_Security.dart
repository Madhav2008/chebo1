// ignore_for_file: file_names, prefer_const_constructors

import 'package:flutter/material.dart';

class PaymentsSecurity extends StatelessWidget {
  const PaymentsSecurity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
          'Payments security',
        ),
      ),
      body: Container(),
    );
  }
}
